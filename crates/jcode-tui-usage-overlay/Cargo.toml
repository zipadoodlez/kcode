[package]
name = "jcode-tui-usage-overlay"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
ratatui = "0.30"
serde = { version = "1", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
